[Journal]
# To constraint how much memory space the journal may use at most
RuntimeMaxUse=64M
# To explicitly disable on-disk storage
# Storage=volatile
# To control how much disk space the journal may use up at most
# Setting this as a fallback if we later enable persistent journal storage
SystemMaxUse=500M
