---
apt_cache_valid_time: 3600
bootstrap_os_packages:
  - apt-transport-https
  - ca-certificates
  - gnupg2
  # We need this for the ansible.builtin.deb822_repository task
  - python3-debian
  - rsync
  - tmux
debian_package_repository_mirrors_file: /etc/apt/mirrors/debian.list
