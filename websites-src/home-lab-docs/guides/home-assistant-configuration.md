# Home Assistant configuration

## Change Network UPS Tools server IP address

To change the NUT server IP address without reconfiguring the Network UPS Tools
(NUT) integration, do the following:

1. Edit the IP address of the NUT server in
   `$HOME_ASSISTANT_CONFIG_DIR/config/.storage/core.config_entries`.
1. Restart Home Assistant.
