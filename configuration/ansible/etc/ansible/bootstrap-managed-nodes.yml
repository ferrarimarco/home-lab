---
- hosts: all
  gather_facts: true
  roles:
    - role: ferrarimarco.home-lab-node
      become: true
    # TODO: use variables to enable or disable these playbooks
    # - role: ferrarimarco.home-lab-windows-node
    # - role: ferrarimarco.docker
    #   become: true
