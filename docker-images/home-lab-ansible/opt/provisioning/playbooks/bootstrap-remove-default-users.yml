---
- hosts: ansible-managed-hosts
  gather_facts: yes
  tasks:
    - name: Remove pre-configured users
      become: yes
      user:
        name: "{{item}}"
        remove: yes
        state: absent
      with_items:
        - ubuntu
